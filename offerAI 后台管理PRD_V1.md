## 1. 概述

### 1.1 背景
- 你在飞书多维表格中，已经通过「八爪鱼 + 飞书AI」构建了较完整的：校招信息表、事业编/国企信息表，以及一批面试资料。
- 现阶段的核心诉求：**用最少的人力维护更多数据**，并且能够安全、稳定地向前台/付费用户提供这套数据。
- 因此，V1 的后台管理主要解决三个问题：
  - **高效维护内容**：飞书定时增量同步 + 后台人工补全；
  - **稳定商业闭环**：会员/资料订单、支付回调、权益发放可视化；
  - **简单可控运营**：能看到关键数据（同步状态、内容发布状态、会员与订单）。

### 1.2 版本范围（本 PRD）
- 仅覆盖「后台管理 + 支撑服务」的 V1：
  - 管理员登录
  - 飞书数据同步配置与任务
  - 校招岗位管理
  - 事业编/国企岗位管理
  - 面试资料管理
  - 会员与订单管理
  - 用户与反馈管理
- 前台展示、投递看板、简历管理等不在本 PRD 详细展开，仅作为依赖说明提及。

### 1.3 目标与不做的事
- **目标**
  - 能在后台**稳定看到并编辑**：校招/事业编岗位、面试资料；
  - 能**一键查看**飞书同步状态、最近一次同步的结果和错误原因；
  - 能快速查到某个用户的会员状态、已购资料、订单及支付情况；
  - 为前台 API 提供可靠的数据源与权限控制。
- **不做**
  - 不做 Web 端邀请裂变（后续在小程序实现）；
  - 不做复杂的 BI 报表，仅做基础列表 + 筛选 + 导出（如需）。

---

## 2. 角色与权限

### 2.1 角色
- **管理员（admin）**
  - 登录后台；
  - 管理飞书同步配置、手动触发同步；
  - 管理校招/事业编岗位、面试资料；
  - 查看用户、会员权益、订单；
  - 处理反馈记录。

### 2.2 权限粒度（简化）
- V1 暂不做细颗粒角色划分，只有一个 `admin` 角色，所有后台页面均可访问。
- 后续如需，可按照模块拆分为：内容运营、财务、客服等。

---

## 3. 模块拆分与页面一览

### 3.1 模块列表
- M1 管理员登录
- M2 飞书数据同步
- M3 校招岗位管理
- M4 事业编/国企岗位管理
- M5 面试资料管理
- M6 会员与订单管理
- M7 用户与反馈管理

### 3.2 页面映射（后台导航）
- 顶部或左侧导航建议结构：
  - 「飞书同步」
  - 「校招岗位」
  - 「事业编岗位」
  - 「面试资料」
  - 「会员与订单」
  - 「用户与反馈」

---

## 4. 模块 PRD：M2 飞书数据同步

> 对应 FeatureList：9.2.x、8.2.x；对应 ER：`飞书同步配置`、`数据同步任务`；对应时序图：时序图06。

### 4.1 用户故事
- US-M2-01：作为管理员，我希望配置飞书表格的 Token、表ID、字段映射，这样系统可以自动从飞书同步数据。
- US-M2-02：作为管理员，我希望能手动触发一次同步，并看到每次同步的新增/更新/失败统计。
- US-M2-03：作为管理员，当某次同步失败时，我希望能在后台看到错误原因，并可以重试失败任务。

### 4.2 主要页面与交互

#### 4.2.1 页面：飞书同步配置列表
- **入口**：导航「飞书同步」→「配置」tab。
- **字段**
  - 数据类型：`校招 / 事业编`
  - 飞书表格ID / 视图ID
  - 同步间隔（分钟）
  - 自动同步开关：开 / 关
  - 上次同步时间
  - 操作：编辑 / 测试连接
- **交互规则**
  - 系统默认两条配置：校招、事业编；不可删除，仅可编辑。
  - 点击「测试连接」：调用后端测试飞书 API 权限，成功则提示「连接成功」，失败展示错误信息。

#### 4.2.2 页面：编辑配置弹窗
- **字段**
  - 飞书AppToken（密文显示，可点击「重置」）
  - 表格ID
  - 视图ID
  - 同步间隔（输入框+单位分钟）
  - 自动同步开关（开关组件）
  - 字段映射配置（可简化为：展示 JSON 只读 + 备注，字段由研发在配置文件维护）
- **交互**
  - 保存后立即生效；
  - 若关闭自动同步，定时任务不再触发，只支持手动同步。

#### 4.2.3 页面：同步任务列表
- **入口**：导航「飞书同步」→「任务」tab。
- **字段**
  - 任务ID
  - 数据类型（校招 / 事业编）
  - 触发方式（auto / manual）
  - 状态（处理中 / 完成 / 失败）
  - 新增/更新/跳过/失败数量
  - 开始时间 / 完成时间
  - 操作：查看详情 / 重试
- **交互规则**
  - 点击「手动同步」按钮：立刻创建一条 `manual` 任务，前端展示 loading 提示，异步执行。
  - 失败任务可点击「重试」，后端按任务记录的时间窗口重新同步，幂等处理。

#### 4.2.4 页面：任务详情侧边栏
- 展示：
  - 任务基础信息（同列表）
  - 错误详情（如飞书接口报错、某条记录字段缺失等）
  - 部分Diff示例（可选，供排查）

### 4.3 业务规则补充
- `来源标签=人工增加` 或 `手动锁定=true` 的记录，飞书增量同步**不得覆盖**。
- 多次同步必须幂等：相同 `source_key` 的记录只更新，不重复创建。

---

## 5. 模块 PRD：M3 校招岗位管理

> 对应 FeatureList：7.3.x、3.x；对应 ER：`校招岗位`；对应时序图：02（前台为结果消费方）。

### 5.1 用户故事
- US-M3-01：作为运营，我希望在后台能按企业/岗位/城市等条件搜索校招岗位，并快速看到来源标签和发布状态。
- US-M3-02：作为运营，我希望能手动新增/编辑岗位（包括企业类型、行业、毕业生要求等），以补全或修正自动同步的数据。
- US-M3-03：作为运营，我希望能一键发布/下架/批量操作岗位，控制前台展示。

### 5.2 页面：校招岗位列表
- **字段**
  - 企业名称
  - 招聘岗位
  - 城市
  - 企业类型
  - 招聘类型
  - 所属行业
  - 来源标签（飞书同步🔄 / 人工增加✏️）
  - 发布状态（已发布/未发布）
  - 生命周期（有效/已过期/已删除）
  - 更新时间
  - 操作：查看 / 编辑 / 发布 / 下架 / 删除
- **筛选条件**
  - 企业名称（模糊）
  - 城市
  - 行业
  - 企业类型
  - 发布状态
  - 来源标签
- **交互要点**
  - 批量勾选行，支持批量发布/下架/删除。
  - 删除为软删除：`生命周期=DELETED`，可在“已删除”筛选中恢复。

### 5.3 页面：校招岗位编辑页
- **基础信息区**
  - 企业名称（必填）
  - 企业类型（下拉：国企/民企/合资/外企/事业单位/公益性）
  - 招聘类型（多选Tag：26届春招/26届秋招/暑期实习/日常实习…）
  - 工作城市
  - 所属行业（多选/单选，枚举同 FeatureList）
- **时间与要求区**
  - 网申开始日期
  - 网申截止日期
  - 毕业生要求（多行文本，保留数据源格式）
- **链接区**
  - 信息来源（文本，如“XX企业公众号”）
  - 投递链接（URL，加密存储）
  - 原文链接（URL，加密存储）
- **运营控制区**
  - 来源标签（只读：飞书同步 / 人工增加）
  - 发布状态（草稿/已发布）
  - 手动锁定开关（开启后飞书同步不覆盖）
- **交互规则**
  - 新增记录默认：来源标签=人工增加，发布状态=草稿。
  - 手动编辑飞书同步来的记录时，可选择是否同时开启手动锁定。

---

## 6. 模块 PRD：M4 事业编/国企岗位管理

> 对应 FeatureList：7.4.x、4.x；对应 ER：`事业编岗位`。

### 6.1 用户故事（简）
- 列表按公告标题/省份/类型筛选；
- 编辑公告详情、报考要求、岗位列表；
- 控制发布/下架。

### 6.2 页面要点
- 列表字段：公告标题、省份、区域、类型、来源标签、报名开始/截止时间、招聘人数、职位数量、发布状态、更新时间。
- 编辑页字段：公告详情长文本、报考学历要求、报考年龄要求、具体岗位（可用表格形式）、原文链接、来源标签、手动锁定等。

（详细字段可直接沿用 `事业编岗位` 表设计，不在此重复逐项展开。）\n\n---\n\n## 7. 模块 PRD：M5 面试资料管理\n\n> 对应 FeatureList：7.5.x、5.x；对应 ER：`面试资料`、`用户资料权限`。\n\n### 7.1 用户故事（简）\n- 列表查看所有资料的标题、分类、价格、状态、销量；\n- 上传 PDF/DOCX 生成预览页；\n- 设置价格、预览页数、上架/下架；\n- 编辑标题、简介、标签。\n\n### 7.2 页面要点\n- 列表：标题、行业、企业标签、价格、预览页数、状态、销量、最后更新时间、操作(编辑/上架/下架)。\n- 编辑页：\n  - 上传文件或更换文件；\n  - 填写标题、简介、行业、企业标签、标签；\n  - 设置价格、预览页数、状态；\n  - 预览生成进度与结果提示。\n\n---\n\n## 8. 模块 PRD：M6 会员与订单管理\n\n> 对应 FeatureList：7.1.x～7.3.x；对应 ER：`订单`、`会员权益`、`用户优惠券`。\n\n### 8.1 用户故事（简）\n- 查某个用户的所有订单和会员状态；\n- 按时间/商品/渠道查看订单列表；\n- 在极端情况下手动开通/延长会员；\n- 查看支付回调日志（简单版）。\n\n### 8.2 页面要点\n- 订单列表：订单号、用户ID/微信昵称、商品名称、商品类型、金额、支付渠道、状态、创建/支付时间；\n- 订单详情：订单基础信息 + 支付流水（第三方交易号、回调状态、时间）；\n- 会员列表/详情：按用户查看其校招/事业编会员到期时间，提供「手动延长X天」按钮（须记录操作日志）；\n\n---\n\n## 9. 模块 PRD：M7 用户与反馈管理\n\n### 9.1 用户列表\n- 字段：用户ID、微信昵称、注册时间、UTM来源、当前会员类型及到期时间、累计消费金额。\n- 筛选：注册时间范围、会员状态、UTM来源。\n\n### 9.2 反馈列表\n- 字段：反馈ID、用户ID、内容类型(校招/事业编/资料)、内容ID、反馈类型(链接失效/内容错误/其他)、处理状态、创建时间、处理人、处理备注。\n- 流程：\n  - 初始状态=待处理；\n  - 运营点击进入详情，填写备注，将状态改为处理中/已完成。\n\n---\n\n## 10. 后台页面原型说明（供 UI 设计参考）\n\n> 下面是 UI 结构和布局建议，方便你在 UIUXPROMAX 中绘制高保真原型。\n\n### 10.1 通用布局\n- 顶部：LOGO + 系统名称 + 当前管理员信息 + 退出登录按钮；\n- 左侧：垂直导航栏（飞书同步 / 校招岗位 / 事业编岗位 / 面试资料 / 会员与订单 / 用户与反馈）；\n- 右侧主区域：上方筛选区 + 中间列表/表单区 + 底部分页/操作区。\n\n### 10.2 飞书同步页面原型\n- 「飞书同步」下分两个 Tab：\n  - Tab1「配置」：\n    - 顶部说明文案：展示当前同步频率、上次同步时间；\n    - 中间表格：两行（校招、事业编），列含：数据类型、表格ID、视图ID、同步间隔、自动同步开关、上次同步时间、「编辑」「测试连接」按钮；\n    - 右上角：全局按钮「手动同步全部」。\n  - Tab2「任务」：\n    - 顶部：筛选条（数据类型、状态、时间范围、触发方式）；\n    - 中间：任务列表表格；\n    - 点击某行「查看详情」在右侧抽屉展开详情；\n\n### 10.3 校招岗位列表原型\n- 顶部筛选区（两行）：\n  - 第一行：企业名称输入框 + 城市下拉 + 企业类型多选 + 行业多选；\n  - 第二行：招聘类型多选 + 发布状态下拉 + 来源标签单选 + 「重置」「搜索」按钮；\n- 表格列：选中框、多列字段（企业名称/岗位/城市/企业类型/招聘类型/行业/来源标签/发布状态/更新时间/操作）；\n- 列表上方：左侧「新增岗位」按钮，右侧「批量发布/批量下架/批量删除」。\n\n### 10.4 校招岗位编辑原型\n- 分为 4 个信息卡片：\n  1. 基础信息（企业名称、企业类型、招聘类型、城市、行业）；\n  2. 时间与要求（网申开始/截止、毕业生要求多行文本）；\n  3. 链接与来源（信息来源文本、投递链接、原文链接）；\n  4. 运营控制（来源标签只读、发布状态下拉、手动锁定开关）。\n- 底部固定操作条：左侧「删除」(编辑态时)、右侧「取消」「保存」。\n\n### 10.5 事业编岗位编辑原型\n- 结构类似校招，但字段换成：公告标题、省份、区域、类型、公告详情长文本、报名开始/截止时间、招聘人数、职位数量、报考学历/年龄要求、具体岗位表格、原文链接、来源标签/手动锁定等。\n\n### 10.6 面试资料管理原型\n- 列表视图：左上「新增资料」按钮，表格列：标题、行业、企业标签、价格、预览页数、状态、销量、更新时间、操作。\n- 编辑弹窗或独立页：上方显示文件上传组件，下方是元数据表单区（标题、简介、行业、标签、价格、预览页数、状态），右侧展示预览效果。\n\n### 10.7 会员与订单、用户与反馈原型\n- 「会员与订单」：上方 Tab「订单列表」「会员列表」。\n  - 订单列表：表格 + 右侧订单详情抽屉；\n  - 会员列表：按用户展示会员类型及到期时间，操作列有「延长会员」按钮。\n- 「用户与反馈」：上方 Tab「用户列表」「反馈列表」。\n  - 用户列表：支持点击行进入用户详情侧栏，查看其订单、会员、已购资料概览；\n  - 反馈列表：状态颜色标记（待处理=黄色，处理中=蓝色，已完成=绿色）。\n\n---\n\n> 后续如需，我们可以在此 PRD 基础上：\n> - 按模块再拆更细的验收用例；\n> - 为每个后台页面补充具体的组件规范（按钮色、表格密度、交互动效）并在 UIUXPROMAX 中落成完整的设计系统。\n*** End Patch"}) ***!
